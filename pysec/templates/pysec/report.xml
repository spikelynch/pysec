<?xml version="1.0"?>
<quarter company="{{ report.name }}" cik="{{ report.cik }}" quarter="{{ report.quarter }}">
  {% for row in rows %}
  <{{ row.0 }}>{{ row.1 }}></{{ row.0 }}>
  {% endfor %}
</quarter>
