<?xml version="1.0"?>
<reports form="{{ form }}" report="{{ report }}" cik="{{ cik }}">
  {% if error %}
  <error>{{ error }}</error>
  {% endif %}
  {% for name, cik in companies %}
  {% endfor %}
</reports>
