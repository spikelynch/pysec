<?xml version="1.0"?>
<reconciliation company="{{ report.name }}" cik="{{ report.cik }}">
  {% if error %}
  <error>{{ error }}</error>
  {% endif %}
  {% for report in reports %}
  <report quarter="{{ report.quarter }}">
    {% for row in report.rows %}
      <{{ row.0 }}>{{ row.1 }}</{{ row.0 }}>
    {% endfor %}
  </report>
  {% endfor %}
</reports>



{% if repdort %}
<table>
  <tr>
    <td>Company</td>
    <td>{{ report.name }} {{ report.cik}}</td>
  </tr>
  <tr>
    <td>Report</td>
    <td>{{ report.form }}</td>
  </tr>
  <tr>
    <td>Date</td>
    <td>{{ report.date }} {{ report.quarter }}</td>
  </tr>
</table>

<p> starts here:</p>

<div>
  {% autoescape off %}
  {{ rec }}
  {% endautoescape %}
</div>
{% else %}
<p>Error: {{ error }}</p>
{% endif %}
